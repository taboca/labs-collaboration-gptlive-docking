# Inner Browsing source provenance

`inner-browsing/` is copied unchanged from:

`/home/taboca/taboca-meetings/labs-pattern-appcomposer-020-inner-browsing/packages/inner-browsing`

Captured on 2026-09-22. Source repository HEAD at capture:
`4fcf452323d9a6f780d1cb69bee7cfecba9ec1b4`.

The package is `@taboca/inner-browsing` 0.1.0, licensed AGPL-3.0-or-later.
Its original LICENSE, README, package metadata, complete source and tests are
included. No framework implementation was modified for this demo. The demo's
WebSocket transport adapter and task service live outside the vendored package.

To update: review a new upstream package, replace this directory as a unit,
record the revision here, run npm install, then rerun framework, application and
browser tests. Preserve the upstream license.
